// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true, // keeps React strict mode on
  // appDir is NOT needed, Next.js detects /app automatically
};

export default nextConfig;